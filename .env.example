# ━━ 仅本地 `npm run dev`：写入以下变量后，浏览器会直连 DeepSeek（勿把真实 key 提交到公开仓库）
VITE_DEEPSEEK_API_KEY=

# ━━ 线上（Vercel 等）或 `vercel dev`：在部署平台里配置，由 /api/chat 在服务端代发，不会打进前端包
# Vercel：Project → Settings → Environment Variables
DEEPSEEK_API_KEY=
